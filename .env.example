DATABASE_URL=postgresql://pascalhub:pascalhub@postgres:5432/pascalhub
REDIS_URL=redis://redis:6379/0
SECRET_KEY=changeme-in-production
# NEXT_PUBLIC_API_URL is NOT used. The frontend uses Next.js rewrites to proxy
# /api/* to the backend container. Set BACKEND_URL in docker-compose if needed.
