version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pascalhub
      POSTGRES_PASSWORD: pascalhub
      POSTGRES_DB: pascalhub
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "13005:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pascalhub"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    ports:
      - "13006:6379"

  backend:
    build: ./backend
    ports:
      - "13001:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://pascalhub:pascalhub@postgres:5432/pascalhub
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./backend:/app

  worker:
    build: ./backend
    command: celery -A app.worker.celery_app worker --loglevel=info --concurrency=4
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://pascalhub:pascalhub@postgres:5432/pascalhub
      REDIS_URL: redis://redis:6379/0
      DISCOVERY_INTERVAL_HOURS: "6"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./backend:/app

  beat:
    build: ./backend
    command: celery -A app.worker.celery_app beat --loglevel=info --schedule /tmp/celerybeat-schedule
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://pascalhub:pascalhub@postgres:5432/pascalhub
      REDIS_URL: redis://redis:6379/0
      DISCOVERY_INTERVAL_HOURS: "6"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      worker:
        condition: service_started
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "13000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://192.168.1.54:13001
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
